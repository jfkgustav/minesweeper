public class GameSettings {
    protected int rows;
    protected int cols;
    protected int mines;

    public GameSettings(int rows, int cols, int mines) {
        this.rows = rows;
        this.cols = cols;
        this.mines = mines;
    }
}
